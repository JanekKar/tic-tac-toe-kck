package app.gui.labels;

import javax.swing.*;
import java.awt.*;

public class LogoPanel extends JLabel {
    public LogoPanel() {
        setOpaque(true);
        setBackground(Color.black);
    }
}
